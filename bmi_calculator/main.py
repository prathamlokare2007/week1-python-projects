# BMI Calculator
weight=float(input("Enter the  weight in KG:"))       # take input  wirght in kg
height=float(input("Enter the height in meter:"))     # take ip  height in meter

BMI=(weight)/(height**2)                              # formula to calc BMI
print("your BMI is:" ,BMI)                            # printing BMI output
